export class Post2 {
    owner: Array<{id: number, nameTitle: string,
        firstName: string, lastName: string, image: any}>;
    tags: Array<{""}>;
    id: number;
    image: any;
    message: string;
}